import { Text } from '../../Themed';

interface CreateSharedCheckListScreenProps {}

const CreateSharedCheckListScreen = ({}: CreateSharedCheckListScreenProps) => {
  return <Text>Create Shared Check List Screen ---- </Text>;
};

export default CreateSharedCheckListScreen;
